var dogName = "Rex";
var weight = 30;


function bark(dogName, weight) {
    if (weight > 20) {
       console.log(dogName + "WOOF WOOF!");
    } else {
        console.log(dogName + "woof woof!");
    }
}